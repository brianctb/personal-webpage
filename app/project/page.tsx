const metadata = {
    title: 'My Portfolio',
}

export default function Page() {
    return (
        <section>
            <h1 className="section-title">{metadata.title}</h1>
        </section>
    )
}
